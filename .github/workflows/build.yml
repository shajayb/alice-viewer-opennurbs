name: Cross-Platform Build

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        submodules: false

    - name: Install Linux Dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libxinerama-dev libxcursor-dev xorg-dev libgl1-mesa-dev pkg-config ninja-build clang lld
        # Ubuntu runner vcpkg is often just a tool, not a full repo. 
        # But we can try to fetch if it's a repo.
        if [ -d "$VCPKG_INSTALLATION_ROOT/.git" ]; then
          cd "$VCPKG_INSTALLATION_ROOT"
          git fetch --unshallow || true
          git fetch origin master
        fi

    - name: Install Windows Dependencies
      if: runner.os == 'Windows'
      run: |
        choco install ninja llvm
        # Add LLVM to path for clang-cl
        echo "C:\Program Files\LLVM\bin" >> $GITHUB_PATH
        # Ensure Ninja is in path
        echo "C:\ProgramData\chocolatey\bin" >> $GITHUB_PATH
        # Check if vcpkg is a git repo. On GitHub runners it usually is C:\vcpkg
        if (Test-Path "$env:VCPKG_INSTALLATION_ROOT\.git") {
            cd "$env:VCPKG_INSTALLATION_ROOT"
            git fetch --unshallow
            git fetch origin master
        } else {
            Write-Host "VCPKG_INSTALLATION_ROOT is not a git repo or doesn't exist. vcpkg might fail to find baseline if not updated."
        }

    - name: Configure CMake
      shell: bash
      run: |
        if [[ "${{ runner.os }}" == "Windows" ]]; then
          cmake -B build -S . -G Ninja \
          -DCMAKE_C_COMPILER="C:/Program Files/LLVM/bin/clang-cl.exe" \
          -DCMAKE_CXX_COMPILER="C:/Program Files/LLVM/bin/clang-cl.exe" \
          -DCMAKE_MAKE_PROGRAM="C:/ProgramData/chocolatey/bin/ninja.exe" \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_TOOLCHAIN_FILE="$VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" \
          -DVCPKG_TARGET_TRIPLET=x64-windows-static \
          -DVCPKG_OVERLAY_PORTS="${GITHUB_WORKSPACE}/vcpkg-overlay-ports/ports"
        else
          cmake -B build -S . -G Ninja \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_TOOLCHAIN_FILE="$VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" \
          -DVCPKG_OVERLAY_PORTS="${GITHUB_WORKSPACE}/vcpkg-overlay-ports/ports"
        fi

    - name: Build
      shell: bash
      run: cmake --build build --config ${{ matrix.build_type }}

    - name: Post-Build (Linux Strip)
      if: runner.os == 'Linux'
      run: strip build/bin/AliceViewer

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: AliceViewer-${{ matrix.os }}
        path: |
          build/bin/AliceViewer
          build/bin/AliceViewer.exe
        if-no-files-found: error
