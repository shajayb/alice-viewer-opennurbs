<GOAL>
Create a cube and display it such that it matches the SEMANTIC CRITERIA = "A Red cube; displayed from an isometric view; black background; shadow cast in the foreground".
</GOAL>

<PLAN>
You (the Architect) must orchestrate the Executor sub-agent (`.agents/executor.md`) in a deliberate, multi-stage Ping-Pong loop to force self-healing and refinement. Do NOT ask for the final result all at once. Follow this strict sequence:

1. **PHASE 1 (The Setup):** Spawn the Executor and instruct it ONLY to generate a plain Red Cube on a Black Background in the rendering loop (e.g., `src/sketch.cpp`), compile (`ninja -C build`), capture `framebuffer.png`, and programmatically exit. Deliberately withhold instructions about shadows or camera angles.
2. **PHASE 2 (The Visual & Code Interrogation):** When the Executor hands off its first iteration, you MUST review the raw C++ code AND perform a semantic visual analysis of `framebuffer.png`. You must explicitly describe exactly what shapes, colors, and lighting you see in the image.
3. **PHASE 3 (The Forced Rework):** Compare your visual analysis against the full SEMANTIC CRITERIA. Because the initial image will inherently lack an isometric view and a foreground shadow, you MUST reject the Executor's handoff. Issue a strict `REWORK` directive commanding the Executor to fine-tune the camera matrix to isometric, and implement shadow mapping logic.
4. **PHASE 4 (Relentless Refinement):** Continuously interrogate the Executor's updated code and `framebuffer.png` files. If the shadow is not clearly in the foreground, or the view is not perfectly isometric, issue another `REWORK` with mathematical adjustments.
</PLAN>

<SUCCESS_CRITERIA>
You may ONLY halt the loop and return success when BOTH of the following are definitively proven:
1. **COMPILE CRITERIA:** 0 compilation errors, and the Executor's code strictly adheres to the Data-Oriented Design (DOD) paradigm (e.g., zero heap allocations in the hot render loop, no STL vectors resizing).
2. **SEMANTIC CRITERIA:** Your visual analysis of the final `framebuffer.png` undeniably confirms every semantic element: A Red cube; displayed from an isometric view; black background; shadow cast in the foreground.
</SUCCESS_CRITERIA>
