@generalist We are orchestrating a parallel Git worktree deployment with strict IPC hardening for Windows.

**GOAL:** Automate the creation of two independent Git worktrees, validate user prompts, and safely spawn two detached Gemini CLI loops without relying on flaky real-time IPC command injection.

**MASTER PLAN:**
1. **Prompt Validation:** Check your current context for the variables or text associated with "prompt_1" and "prompt_2". If they are not explicitly provided in this request, DO NOT proceed. Halt and ask me to provide them.
2. **Worktree Generation:** Use PowerShell to execute:
   - `git worktree add ../worktree_alpha branch_alpha` (or create the branch if it doesn't exist via `-b`).
   - `git worktree add ../worktree_beta branch_beta`.
3. **IPC Hardening (File-Based Payload):** To avoid shell-escaping and IPC dropouts, write the raw text of "prompt_1" into `../worktree_alpha/payload.txt` and "prompt_2" into `../worktree_beta/payload.txt`.
4. **Bootstrap Script Creation:** Generate two PowerShell scripts (`launch_alpha.ps1` and `launch_beta.ps1`) in the current directory. These scripts must strictly execute:
   - `cd ../worktree_alpha` (or beta)
   - `Get-Content payload.txt | gemini -y`
   - `Pause` (to prevent the window from closing instantly if it crashes).
5. **Detached Execution:** Use `Start-Process powershell.exe -ArgumentList "-NoExit -File launch_alpha.ps1"` (and equivalent for beta) to spawn the new, independent terminal windows.

**DIRECTIVE:**
Act as the Orchestrator. Read `.agents/architect.md` and `.agents/executor.md`. Pass the GOAL and MASTER PLAN to the Executor context. Instruct the Executor to strictly use PowerShell syntax for all operations. Do not attempt to use `cmd.exe`. Do not halt until the worktrees are verified to exist and the detached processes have been successfully spawned.