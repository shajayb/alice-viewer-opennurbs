[CLI AGENT PROMPT]
CONTEXT: 
Initialize according to `gemini.md`. The try/catch block successfully prevented a hard crash! However, we hit a `nlohmann::json` exception (`invalid UTF-8 byte: 0xFF`). A legacy UTF-16 Byte Order Mark (BOM) was left in `executor_console.log` by a previous PowerShell pipe. We must sanitize all file reads in the Orchestrator.

CRITICAL SYSTEM RULES: 
1. FILE LOCK AVOIDANCE: Specify your build targets explicitly. Rename running `.exe` files to `.old.exe` before linking if necessary.

EXECUTION MANDATE:
Execute SOP Steps 1 through 7. Write the strict JSON report to `executor_report.json`.

1. PHASE 1 (The Orchestrator Sanitizer): 
   - Open `src/AliceOrchestrator.cpp`.
   - Create a helper function `std::string sanitizeString(const std::string& input)` above `readFileContents`.
   - In `sanitizeString`, iterate through the input and remove any `0xFF`, `0xFE`, or `0x00` (null) bytes. (Alternatively, just strip anything where `(unsigned char)c > 127` to force pure ASCII for our logs and prompts).
   - Update `readFileContents` to return `sanitizeString(content)` so all files read from disk are automatically stripped of illegal UTF-8 bytes and BOMs.
   - Build explicitly: `ninja AliceOrchestrator`

2. PHASE 2 (File Wiping): 
   - Open `src/TilesetMicroTest.cpp`.
   - Before opening the `std::ofstream`, add `std::filesystem::remove("executor_console.log");` to explicitly destroy the old, corrupted file on disk.
   - Build explicitly: `ninja TilesetMicroTest`
   - Run the test natively (`./build/bin/TilesetMicroTest.exe`).

3. PHASE 3 (TERMINATION):
   - Write the `executor_report.json` detailing the sanitization fix.
   - Do NOT execute any taskkill commands yourself. The Orchestrator's Watchdog will terminate you.

Run autonomously. Do not ask for permission.